--[[
    Future update. 
    Walks around the farm until it finds a container, resets gps. Only executed when a gps return method does not result 
    back to the origin point (Yield container)
]]

function Resume()
    --locate itself by looking for a container, store items and get fuel if needed
end